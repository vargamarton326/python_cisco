#def fun(in=2, out=3):
 #   return in*out

#print(fun(3))

tup = (1, ) + (1, )
tup = tup + tup
print(len(tup))


value=input("value:")
print(10/value)

#def , must be placed before the 1 invocation , 
# only inside the function , positional ,
# RETURN: the function's execution to reterminate-may cause the fun... ,
# a None value , may be read, butnot written..., will affect the argument 
# the snippet is errorneous... , 4 , 4.0, TypeError

